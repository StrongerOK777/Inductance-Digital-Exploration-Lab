
### 2026-05-24 修改记录
1. **修改文件**：`client/src/index.css`
   - **改动内容**：将浅色和深色主题的主色调（`--lab-primary`）改为高级蓝色（#38bdf8 / #0ea5e9），将强调色（`--lab-secondary`, `--lab-success`）改为绿色（#34d399 / #059669），以使界面更加高级并在克制的地方呈现点缀。
   - **结果**：整体界面的颜色风格完成了优化，变得更加现代化和高级。
2. **修改文件**：`client/src/components/MagneticFieldViewer.tsx`, `client/src/pages/Home.tsx`, `client/src/components/ControlPanel.tsx`
   - **改动内容**：将“磁力线密度”的含义重新定义为“360度下一共几条磁力线”，修改了对应的拉动条范围为 4 到 48，默认值设置为 24 条，并将底层渲染中生成的副本数（copies）直接与 density 对齐。
   - **结果**：用户现在可以通过滑块精确控制生成多少条经过原点的磁力线。
3. **修改文件**：`client/src/components/MagneticFieldViewer.tsx`
   - **改动内容**：在渲染连续磁场线（原采用 `Line2` 的多段折线模型）的逻辑中，取消了宽线的连线绘制，转换为通过 `THREE.InstancedMesh` 呈现高密度的向量（矢状圆柱+圆锥组合体）来勾勒原本的磁力线路径。此外去除了 `three/examples/jsm/lines/` 系列的导入。
   - **结果**：解决了磁力线呈现为折线段且可能产生视觉重叠、打结的问题，所有磁力线均以清晰的物理矢量分布呈现，更加专业和物理直观。


4. **修改文件**：`client/src/components/MagneticFieldViewer.tsx`
   - **改动内容**：将磁场线的渲染逻辑从“分离的矢量箭头”恢复为连续的曲线（`Line2`），并大幅增加了样条曲线（`CatmullRomCurve3`）的插值采样点数量（上限增至 8000，系数增至 20 倍）。同时保留了路径上稀疏排布的小箭头以指示方向。
   - **结果**：曲线现在是以基于屏幕空间宽度（screen-space width）的抗锯齿着色器级矢量绘制的。无论用户视角放大多少倍，曲线都能保持原粗细且依然完美平滑，彻底解决了放大后呈现折线段（多边形边缘）的问题，满足了用户期待的“矢量图”级别的画质。


5. **修改文件**：`client/src/components/MagneticFieldViewer.tsx`
   - **改动内容**：重写了 `smoothLinePoints` 函数。移除了向 `CatmullRomCurve3` 提供控制点前进行的线性插值重采样（`resampleFieldLine`），直接使用物理引擎（RK4 积分器）结算出的高精度非均匀点作为插值控制点。并添加了过滤极近重合点与恢复默认向心参数化策略的逻辑。
   - **结果**：成功修复了使用样条曲线渲染物理轨迹时在此前由于“长线段上的共线控制点遇到转折要求强制维持C1连续性”所导致的大量非物理“自交打结（loops/knots）”与异常过冲现象。现在磁力线无论是平滑性、精细度还是物理准确性层面都表现完美。

\n## 2026-05-24 21:37:55\n- 文件: `client/src/lib/physics.ts`\n- 改动内容: 将磁场积分计算的毕奥-萨伐尔定律由原来的左点矩形近似数值积分，修改为离散直线段线框精确解析计算公式（消除了微小尺度的离散化数值误差，从而解决了双线圈靠近时磁感线的非物理绕圈现象）。\n- 结果: 成功。

## `client/src/lib/physics.ts` 改动记录
- **时间**: $(date "+%Y-%m-%d %H:%M:%S")
- **文件**: `client/src/lib/physics.ts`
- **改动内容**: 
  - 将磁场的数值积分方法从基本的左点矩形近似（`dl × r / |r|³`）重写为了有限直线段的精确解析公式（Analytical Biot-Savart formula for straight wire segments）。
  - 处理了在磁感线上坐标与线框极近位置可能发生的数值计算奇点。
- **结果**: 彻底消除了由于离散化近似误差在近场空间（双线圈间距较近时）累积产生的磁感线扭曲与非物理小圈绕场现象。计算精度达到了解析解级别。

## `client/src/lib/physics.ts` 改动记录（修复打结复现问题）
- **时间**: $(date "+%Y-%m-%d %H:%M:%S")
- **文件**: `client/src/lib/physics.ts`
- **改动内容**: 
  - 发现前一次修改的有限元解析公式在磁感线跨过线圈多边形线段的“延长线”时（如双线圈 z=0 附近的网格射线面），依然会触发 `0/0` 形式的浮点数灾难性抵消误差（Catastrophic Cancellation）。
  - 通过代数化简工具（将公式中的 $(r_1^2 \cdot r_2^2 - (r_1\cdot r_2)^2)$ 完美分解为 $(r_1 r_2 - r_1\cdot r_2)(r_1 r_2 + r_1\cdot r_2)$ 并将之从余弦差分分子式中约掉），彻底抽离了微小浮点数相除产生的巨大数值噪音部分。
  - 将毕奥-萨伐尔定律针对直线段的算法，重构为无需计算距离叉乘平方的极致稳健版本。
- **结果**: 对比前后双线圈中间的畸变区域轨迹，这次彻底从数学和计算机浮点精度底层阻断了 `B` 矢量噪音，无论跟踪器在三维空间多接近线圈段的延长线或是中央对冲区域，均不会引发折转和乱麻绕圈现象。
